# starter-server

### Requirements

- Node.js
- npm/Yarn
- Docker

### Development

Start database, adminer and keycloak:

```
$ docker-compose up
```

Start server in development:

```
$ npm start
```

Build and start server in production:

```
$ npm run start:prod
```
